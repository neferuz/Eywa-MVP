# 🚀 ЗАПУСК CRM EYWA - ИНСТРУКЦИЯ

## ⚡ Быстрый старт (2 команды)

### 1. Backend
```bash
cd backend
bash start_server.sh
```

### 2. Frontend (в новом терминале)
```bash
cd frontend
npm run dev
```

**Готово!** Откройте http://localhost:3001

---

## 📋 Что нужно перед запуском

### Обязательно:
- ✅ Python 3.8+
- ✅ Node.js 18+
- ✅ PostgreSQL (или сервер запустится с моками)

### Опционально (для AI функций):
- ✅ OpenAI API ключ (или Timeweb Cloud AI)
- ✅ ElevenLabs API ключ (для качественного голоса)

---

## 🔧 Первый запуск

### Backend

1. **Установите зависимости:**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

2. **Настройте .env** (если еще не настроен):
   ```bash
   # Создайте backend/.env с:
   DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/eywa
   ELEVENLABS_API_KEY=sk_40b82c8f085107b551eef776ddcbbaea2a77cb902c2a4c43
   ELEVENLABS_VOICE_ID=aG9q1I1wTbfHh5sbpJnp
   TIMEWEB_AGENT_ACCESS_ID=bbd689cc-3715-4771-9843-2cca24481ad8
   ```

3. **Примените миграции:**
   ```bash
   alembic upgrade head
   ```

4. **Запустите:**
   ```bash
   bash start_server.sh
   ```

### Frontend

1. **Установите зависимости:**
   ```bash
   cd frontend
   npm install
   ```

2. **Запустите:**
   ```bash
   npm run dev
   ```

---

## ✅ Проверка

1. Backend: http://localhost:8000/health → `{"status":"ok"}`
2. Frontend: http://localhost:3001 → Открывается CRM
3. API Docs: http://localhost:8000/docs → Swagger UI

---

## 🎙️ Голосовой ассистент

После запуска:
1. Войдите в CRM
2. Найдите кнопку с микрофоном (внизу справа)
3. Нажмите и начните общаться!

**Примеры вопросов:**
- "Сколько клиентов в базе?"
- "Какая выручка за сегодня?"
- "Привет"

---

## 📚 Документация

- `QUICK_START.md` - Подробная инструкция
- `PROJECT_STATUS.md` - Статус проекта
- `AI_ASSISTANT_SETUP.md` - Настройка AI
- `TIMEWEB_SETUP.md` - Настройка Timeweb
- `ELEVENLABS_SETUP.md` - Настройка ElevenLabs

---

## 🆘 Проблемы?

### Порт занят
```bash
# Backend (8000)
lsof -ti:8000 | xargs kill -9

# Frontend (3001)
lsof -ti:3001 | xargs kill -9
```

### Зависимости не установлены
```bash
# Backend
cd backend && pip install -r requirements.txt

# Frontend
cd frontend && npm install
```

### База данных
- Сервер работает и без БД (использует моки)
- Для реальных данных настройте `DATABASE_URL` в `.env`

---

## 🎯 Всё готово!

Проект полностью настроен и готов к использованию.

**Начните с:**
1. Запустите backend
2. Запустите frontend
3. Откройте http://localhost:3001
4. Начните работать! 🚀

