# 🔄 Перезапуск сервера для применения настроек ElevenLabs

## Важно: Перезапустите сервер!

После добавления переменных в `.env` нужно **обязательно перезапустить сервер**, чтобы настройки применились.

## Как перезапустить

### Способ 1: Остановить и запустить заново

1. Остановите текущий сервер (Ctrl+C в терминале)
2. Запустите снова:
   ```bash
   cd backend
   uvicorn app.main:app --reload
   ```

### Способ 2: Если сервер запущен в фоне

```bash
# Найти процесс
ps aux | grep uvicorn

# Остановить (замените PID на номер процесса)
kill -9 PID

# Запустить снова
cd backend
uvicorn app.main:app --reload
```

## Проверка

После перезапуска:
1. Откройте CRM в браузере
2. Нажмите на кнопку с микрофоном
3. Задайте вопрос
4. Голос должен звучать через ElevenLabs (естественно, не как робот)

## Текущие настройки

В вашем `.env`:
- `ELEVENLABS_API_KEY=sk_40b82c8f085107b551eef776ddcbbaea2a77cb902c2a4c43` ✅
- `ELEVENLABS_VOICE_ID=aG9q1I1wTbfHh5sbpJnp` ✅

Всё настроено правильно! Просто перезапустите сервер.

