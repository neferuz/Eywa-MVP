# 🎙️ AI Голосовой Ассистент для CRM

## Описание

AI-голосовой помощник для CRM системы, который позволяет администраторам и владельцам бизнеса получать информацию о состоянии бизнеса с помощью голосовых команд и текстовых запросов.

## Возможности

✅ **Голосовой ввод** - распознавание речи через Web Speech API  
✅ **Голосовой ответ** - озвучивание ответов через Web Speech API  
✅ **Текстовый ввод** - возможность вводить вопросы вручную  
✅ **История диалога** - сохранение контекста разговора  
✅ **Доступ к данным CRM** - получение статистики в реальном времени  
✅ **Безопасность** - доступ только для авторизованных пользователей  

## Технологии

### Backend
- FastAPI
- OpenAI GPT-4o-mini (для обработки запросов)
- PostgreSQL (для получения данных CRM)
- httpx (для запросов к OpenAI API)

### Frontend
- Next.js 16
- React 19
- Web Speech API (Speech Recognition + Speech Synthesis)
- TypeScript

## Установка и настройка

### 1. Backend

#### Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

#### Настройка переменных окружения

Создайте файл `.env` в папке `backend/` или добавьте в существующий:

```env
# OpenAI API ключ (обязательно)
OPENAI_API_KEY=your_openai_api_key_here

# Остальные настройки (если еще не настроены)
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/eywa
SECRET_KEY=your-secret-key
```

**Важно:** Для работы AI ассистента необходим OpenAI API ключ. Получить его можно на [platform.openai.com](https://platform.openai.com/api-keys)

#### Запуск сервера

```bash
cd backend
uvicorn app.main:app --reload
```

### 2. Frontend

Зависимости уже установлены (Web Speech API встроен в браузер).

#### Запуск

```bash
cd frontend
npm run dev
```

## Использование

### Интерфейс

1. **Плавающая кнопка** - в правом нижнем углу экрана появится кнопка с иконкой микрофона
2. **Открытие чата** - нажмите на кнопку, чтобы открыть окно ассистента
3. **Голосовой ввод** - нажмите на кнопку микрофона и говорите
4. **Текстовый ввод** - введите вопрос в текстовое поле и нажмите Enter или кнопку отправки
5. **Управление голосом** - кнопка с иконкой динамика позволяет включить/выключить озвучивание ответов

### Примеры вопросов

#### Общие вопросы
- "Привет"
- "Как дела?"
- "Что нового?"

#### Вопросы о клиентах
- "Сколько клиентов в базе?"
- "Сколько новых клиентов за сегодня?"
- "Сколько активных клиентов?"
- "Сколько новых клиентов за неделю?"
- "Сколько новых клиентов за месяц?"

#### Вопросы о записях
- "Сколько записей сегодня?"
- "Есть ли свободные слоты сегодня?"

#### Вопросы о выручке
- "Какая выручка за сегодня?"
- "Какая выручка за неделю?"
- "Какая выручка за месяц?"

### Поддерживаемые браузеры

- ✅ Chrome/Edge (полная поддержка Web Speech API)
- ✅ Safari (частичная поддержка)
- ⚠️ Firefox (ограниченная поддержка)

**Рекомендуется использовать Chrome или Edge для лучшей работы голосового ввода.**

## API Endpoints

### POST `/api/ai-assistant/chat`

Отправка сообщения AI ассистенту.

**Запрос:**
```json
{
  "message": "Сколько клиентов в базе?",
  "conversation_history": [
    {
      "role": "user",
      "content": "Привет"
    },
    {
      "role": "assistant",
      "content": "Привет! Рад вас видеть. Готов помочь с аналитикой или вопросами по CRM 😊"
    }
  ]
}
```

**Ответ:**
```json
{
  "message": "В базе 150 клиентов, из них 120 активных.",
  "data": {
    "total_clients": 150,
    "active_clients": 120,
    "new_clients_today": 5,
    "today_bookings": 12,
    "revenue_today": "250 000",
    ...
  }
}
```

### GET `/api/ai-assistant/stats`

Получить статистику CRM (для отладки).

**Ответ:**
```json
{
  "total_clients": 150,
  "active_clients": 120,
  "new_clients_today": 5,
  "new_clients_week": 25,
  "new_clients_month": 100,
  "today_bookings": 12,
  "revenue_today": 250000.0,
  "revenue_week": 1500000.0,
  "revenue_month": 5000000.0,
  "available_slots_today": 8
}
```

## Безопасность

- ✅ Все запросы требуют авторизации (JWT токен)
- ✅ Доступ к данным ограничен ролью пользователя
- ✅ Все запросы логируются
- ✅ API ключ OpenAI хранится только на сервере

## Настройка AI ассистента

Системный промпт ассистента можно изменить в файле:
`backend/app/api/routes/ai_assistant.py` в функции `get_system_prompt()`

## Устранение неполадок

### Проблема: "Speech Recognition API не поддерживается"

**Решение:** Используйте Chrome или Edge браузер. Firefox имеет ограниченную поддержку.

### Проблема: "Разрешите доступ к микрофону"

**Решение:** 
1. Проверьте настройки браузера - разрешите доступ к микрофону
2. Убедитесь, что сайт открыт по HTTPS (или localhost)

### Проблема: "AI-ассистент временно недоступен"

**Решение:**
1. Проверьте, что `OPENAI_API_KEY` установлен в `.env` файле
2. Убедитесь, что ключ валидный и имеет доступ к API
3. Проверьте баланс на аккаунте OpenAI

### Проблема: Ассистент не понимает вопросы

**Решение:**
1. Говорите четко и медленно
2. Используйте простые формулировки
3. Проверьте, что микрофон работает корректно

## Развитие функционала

### Планируемые улучшения:
- [ ] Поддержка узбекского языка
- [ ] Проактивные подсказки
- [ ] AI-аватар
- [ ] Экспорт отчетов через голосовые команды
- [ ] Интеграция с Telegram/WhatsApp
- [ ] Кеширование частых запросов

## Лицензия

Внутренний проект для CRM системы Eywa.

